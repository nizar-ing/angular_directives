import { CustomClassDirective } from './custom-class.directive';

describe('CustomClassDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomClassDirective();
    expect(directive).toBeTruthy();
  });
});
